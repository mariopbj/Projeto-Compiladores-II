PROGRAM teste;

VAR
    x : INTEGER;
    y : INTEGER;
    z : INTEGER;

BEGIN

READ(x);
READ(y);

z := x + y;

IF ((z > 10) OR ~(x == y)) THEN {

    WRITE(z)

};

WHILE (z > 0) DO {

    z := z - 1;
    WRITE(z)

}

END.